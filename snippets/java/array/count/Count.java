package array;

public class Count {

    public static void main(String args[]) {
       int[] array = new int[]{1,2,3};
       int arraySize = array.length;  // ≡
       System.out.println("the length of the array is "+arraySize); // prints: the length of the array is 3
    }
}

using System;

public class Count {         
    static void Main(string[] args)
    {
        int[] array = { 1, 2, 3 };
        Console.WriteLine("the length of the array is {0}", array.Length);
    }
}
